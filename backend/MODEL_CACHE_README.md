# 🤖 AI 모델 캐시 관리 가이드

## 📁 모델 캐시 디렉토리

임베딩 모델은 `backend/model_cache/` 폴더에 저장됩니다.

### 📍 경로
```
backend/
└── model_cache/
    └── sentence-transformers/
        └── jhgan_ko-sroberta-multitask/
            ├── pytorch_model.bin (약 443MB)
            ├── config.json
            ├── tokenizer.json
            └── ...
```

---

## 🚀 첫 실행 (모델 다운로드)

### 첫 실행 시 자동 다운로드
백엔드를 처음 실행하면 모델이 자동으로 다운로드됩니다:

```bash
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

**다운로드 시간**: 약 2-5분 (443MB)

다운로드 진행 상황:
```
🔄 임베딩 모델 로드 중: jhgan/ko-sroberta-multitask
📁 모델 캐시 경로: ./backend/model_cache
Downloading pytorch_model.bin: 100%|████████| 443M/443M [02:30<00:00, 2.95MB/s]
✅ 벡터 스토어 초기화 완료
```

---

## 💾 모델 캐시 백업 및 복사

### 방법 1: 다른 환경으로 복사 (권장)

모델을 한 번 다운로드한 후, 다른 컴퓨터나 환경에서 재사용:

#### Windows → Windows
```bash
# 1. 모델 캐시 폴더 압축
cd "G:\내 드라이브\11. DEV_23\51. Python_mp3등\BH2025_WOWU\backend"
tar -czf model_cache_backup.tar.gz model_cache/

# 2. 다른 환경에 복사 후 압축 해제
tar -xzf model_cache_backup.tar.gz
```

#### 또는 그냥 폴더 복사
```bash
# 전체 model_cache 폴더를 복사
robocopy "G:\...\BH2025_WOWU\backend\model_cache" "D:\...\BH2025_WOWU\backend\model_cache" /E
```

### 방법 2: Google Drive 등에 백업

```bash
# 1. model_cache 폴더를 압축
tar -czf model_cache_backup.tar.gz backend/model_cache/

# 2. Google Drive에 업로드
# (model_cache_backup.tar.gz 파일을 Google Drive에 수동 업로드)

# 3. 다른 환경에서 다운로드 후 압축 해제
tar -xzf model_cache_backup.tar.gz
```

---

## 📦 캐시 위치 확인

### Windows
```bash
dir backend\model_cache\sentence-transformers\jhgan_ko-sroberta-multitask
```

### Linux/Mac
```bash
ls -lh backend/model_cache/sentence-transformers/jhgan_ko-sroberta-multitask/
```

---

## 🔄 모델 재다운로드

캐시를 삭제하고 모델을 다시 다운로드하려면:

### Windows
```bash
rmdir /s /q backend\model_cache
```

### Linux/Mac
```bash
rm -rf backend/model_cache
```

그 후 백엔드를 재시작하면 자동으로 다운로드됩니다.

---

## 💡 팁

### 1️⃣ 한 번 다운로드, 여러 번 재사용
- 첫 실행 시 다운로드 (2-5분)
- 이후 실행은 즉시 로드 (< 5초)
- `model_cache` 폴더를 백업해두면 재사용 가능

### 2️⃣ 팀 공유
- 팀원 중 한 명이 다운로드
- `model_cache` 폴더를 압축하여 공유
- 다른 팀원은 압축 해제 후 바로 사용

### 3️⃣ Git에는 포함되지 않음
- `.gitignore`에 `backend/model_cache/` 추가됨
- 용량이 크므로 Git에 커밋하지 않음
- 별도로 백업 및 공유

### 4️⃣ 네트워크 제한 환경
- 오프라인 환경에서는 사전에 다운로드
- USB나 외장하드로 `model_cache` 폴더 복사
- 네트워크 연결 없이도 사용 가능

---

## 📊 모델 정보

### jhgan/ko-sroberta-multitask
- **크기**: 약 443MB
- **용도**: 한국어 문장 임베딩
- **특징**: 한국어 의미 검색 최적화
- **라이센스**: MIT
- **출처**: https://huggingface.co/jhgan/ko-sroberta-multitask

---

## 🔧 문제 해결

### 다운로드가 매우 느린 경우
1. HuggingFace 미러 사이트 사용
2. 다른 네트워크 환경에서 다운로드
3. VPN 사용

### 다운로드 중 오류 발생
1. 네트워크 연결 확인
2. 디스크 공간 확인 (최소 1GB 필요)
3. 다운로드 재시도 (Ctrl+C로 중단 후 재실행)

### 모델 로드 오류
```python
OSError: Can't load the model
```
- `model_cache` 폴더 삭제 후 재다운로드
- 폴더 권한 확인

---

**작성일**: 2024-12-30  
**버전**: 1.0  
**문의**: BH2025 WOWU 시스템 관리자
